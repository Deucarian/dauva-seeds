throw new Error(
  "Direct Seed promotion is disabled by the approved Seed Studio specification. Generate a signed deterministic release bundle, verify it, and apply it through the protected Git workflow.",
);
